begin to implement ui aspects

allow cmd to be called outside of pollen dir, set with env vars (ask/error if env vars not set)
env vars
POLLEN_DEFAULT_DIR=undefined
POLLEN_ROOT=$HOME

impl backups, undo command
will require subdirectory support
cache/
files/
scripts/
track.yaml

impl "depends_on" option
